# Money Transfer System

Система для безопасных денежных переводов между счетами с многопоточностью.

## 📋 Описание

Приложение реализует систему денежных переводов между счетами с использованием многопоточности. Создается несколько счетов и потоков, которые выполняют случайные переводы между счетами с соблюдением безопасности и целостности данных.

## 🚀 Функциональность

- Создание нескольких счетов с начальным балансом
- Запуск независимых потоков для выполнения переводов
- Безопасные транзакции с предотвращением deadlock
- Логирование всех операций в консоль
- Автоматическое завершение после заданного количества транзакций

## 🛠 Технологии

- **Java 17**
- **Gradle** - система сборки
- **java.util.logging** - логирование
- **Многопоточность** - ReentrantLock, CountDownLatch
- **Синхронизация** - упорядоченные блокировки для предотвращения deadlock

## ⚙️ Конфигурация

### Основные параметры (изменяются в [Main.java](src/main/java/ru/liquisort/testAssignment/Main.java)):

```java
private static final int INITIAL_BALANCE = 10000;    // Начальный баланс счетов
private static final int NUMBER_OF_ACCOUNTS = 4;     // Количество счетов
private static final int NUMBER_OF_THREADS = 2;      // Количество потоков
private static final int MAX_TRANSACTIONS = 30;      // Максимум транзакций
```

```
# Сборка проекта
./gradlew build

# Запуск приложения
./gradlew run

# Только компиляция
./gradlew compileJava

# Очистка проекта
./gradlew clean
```

